# 昔涟(Cyrene) Agent 核心架构解析

> 本文档详细解析昔涟 Agent 的三个核心架构设计：
> 1. 不同 LLM 厂商的 API 切换机制
> 2. Embedding 实现
> 3. 天气获取工具实现

---

## 一、不同 LLM 厂商的 API 切换

### 1.1 架构概览

采用的是**适配器模式 (Adapter Pattern)**，核心架构分为三层：

```
用户设置 (ModelSettings)
  ├─ provider: "DeepSeek（深度求索）"
  ├─ baseUrl: "https://api.deepseek.com"
  ├─ model: "deepseek-v4-pro"
  └─ apiKey: "sk-..."
        │
        ▼
  LlmClient.buildVendorConfig()
        │
        ▼
  getAdapterForConfig(cfg)
        ├─ anthropic transport → AnthropicAdapter
        └─ openai transport    → OpenAICompatAdapter
                │
                ▼
        adapter.buildStreamRequest()
        → fetch(url, { headers, body })
        → createSseReader() 统一解析流
        → adapter.parseStreamEvent() 解析增量
```

三层架构：

- **Layer 1 — 能力表 (Capability Table)**：`src/main/orchestrator/vendors/capabilities.ts` — 所有厂商元数据的唯一事实来源
- **Layer 2 — 适配器工厂 (Factory)**：`src/main/orchestrator/vendors/index.ts` — 按 transport 路由到正确的适配器
- **Layer 3 — 适配器实现 (Adapters)**：`openai-adapter.ts` / `anthropic-adapter.ts` — 实际的 wire protocol 翻译

### 1.2 厂商能力表

所有厂商的元数据在一张表里集中定义，共支持 **9 家厂商**：

| 厂商 ID | 显示名 | Transport | 鉴权方式 | 默认模型 |
|---|---|---|---|---|
| `minimax` | MiniMax（稀宇科技） | `openai` | `bearer` | MiniMax-M3 |
| `deepseek` | DeepSeek（深度求索） | `openai` | `bearer` | deepseek-v4-pro |
| `doubao` | 豆包（火山方舟） | `openai` | `bearer` | doubao-seed-2-1-pro |
| `glm` | GLM（智谱） | `openai` | `bearer` | glm-5.2 |
| `kimi` | Kimi（月之暗面） | `openai` | `bearer` | kimi-k2.7-code |
| `qwen` | Qwen（通义千问） | `openai` | `bearer` | qwen-max |
| `chatgpt` | ChatGPT（OpenAI） | `openai` | `bearer` | (用户自定义) |
| `claude` | Claude（Anthropic） | `anthropic` | `x-api-key` | claude-sonnet-4-6 |
| `mimo` | MiMo（小米） | `openai` | `bearer` | mimo-v2.5-pro |

关键文件：[capabilities.ts](src/main/orchestrator/vendors/capabilities.ts)

每个厂商的能力声明包括：

```typescript
interface ProviderCapability {
  id: string;              // 内部标识
  displayName: string;     // UI 显示名
  transport: Transport;    // "openai" | "anthropic"
  baseUrl: string;         // 默认 API 地址
  authStyle: AuthStyle;    // "bearer" | "x-api-key"
  defaultModel: string;
  supportsTools: boolean;
  supportsThinking: boolean;
  thinkingField: ThinkingField;    // "reasoning_content" | "thinking" | "reasoning_details" | null
  cacheStrategy: CacheStrategy;    // "prompt_cache_key" | "cache_control" | "auto" | "none"
  supportsVision: boolean;
  visionBaseUrl?: string;          // 多模态独立入口
  toolChoiceModes?: ReadonlyArray<"named" | "required" | "auto" | "omit">;
}
```

未知厂商自动回退为 OpenAI 兼容兜底（`getCapabilityOrOpenAI()`）。

### 1.3 适配器工厂

关键文件：[vendors/index.ts](src/main/orchestrator/vendors/index.ts)

```typescript
// 按运行时配置取适配器实例
export function getAdapterForConfig(cfg: VendorConfig): ChatVendorAdapter {
  const transport = resolveTransport({
    baseUrl: cfg.baseUrl,
    explicitTransport: cfg.explicitTransport,
    provider: cfg.provider,
  });
  const cacheKey = `${cfg.provider}::${transport}`;
  const existing = cache.get(cacheKey);
  if (existing) return existing;
  const cap = getCapabilityOrOpenAI(cfg.provider);
  const adapter: ChatVendorAdapter =
    transport === "anthropic"
      ? new AnthropicAdapter(cap.id, cap)
      : new OpenAICompatAdapter(cap.id, cap);
  cache.set(cacheKey, adapter);
  return adapter;
}
```

核心逻辑：**只看 transport 字段**，不看厂商名。`resolveTransport()` 允许用户显式选择协议，覆盖厂商默认值（比如 MiniMax 默认走 OpenAI 兼容协议，但用户可以强制切到 Anthropic 协议入口）。

适配器按 `${provider}::${transport}` 缓存，避免切换 transport 后命中旧实例。

### 1.4 两种 Transport 的差异

**OpenAI transport** — `openai-adapter.ts`：
- 请求端点：`POST {baseUrl}/chat/completions`
- 消息格式：标准 OpenAI JSON（`messages`、`tools[].type=function`、`tool_choice`、`response_format`、`stream`）
- 流式格式：`data: {...}` SSE
- 鉴权：`Authorization: Bearer` 或 `x-api-key`（按 capability 表）
- 思考链提取：从 `reasoning_content`、`thinking` 或 `reasoning` 字段

**Anthropic transport** — `anthropic-adapter.ts`：
- 请求端点：`POST {baseUrl}/v1/messages`
- 消息格式：Anthropic JSON（`system` 置顶、`messages[].content` 为 content block 数组、`tools[].input_schema`）
- 流式格式：`event: <type>\ndata: {...}` event-stream
- 鉴权：`x-api-key` + `anthropic-version: 2023-06-01`
- 多轮回传：`rawAssistant` 保留原始 content block 数组在下轮原样回传（MiniMax Anthropic 入口的硬要求）

两种协议的流式解析共用同一套 `createSseReader` 状态机（按 `\n\n` 切分 event 块），adapter 的 `parseStreamEvent` 是纯函数，无缝切换。

### 1.5 Transport 自动检测

关键文件：[transport-detector.ts](src/main/orchestrator/vendors/transport-detector.ts)

**仅尊重用户显式选择**，不从 baseUrl 推断：
- 用户选了 `"openai"` → 走 OpenAI transport
- 用户选了 `"anthropic"` → 走 Anthropic transport
- 旧配置 `"auto"` 或缺失 → 回退 capability 表默认值
- **绝不在运行时猜协议**

### 1.6 鉴权解耦

关键文件：[auth.ts](src/main/orchestrator/vendors/auth.ts)

```typescript
export function authHeaderFor(cap, apiKey, transport): { key: string; value: string } {
  // Anthropic transport + capability 声明了 anthropicAuthStyle → 优先用
  // 否则按 capability.authStyle
  // bearer → Authorization: Bearer {apiKey}
  // x-api-key → x-api-key: {apiKey}
}
```

鉴权方式与 transport 解耦：MiniMax 的 `/anthropic` 入口可以用 `x-api-key`，同时其 OpenAI 入口用 `bearer`。

### 1.7 端点 URL 构建

关键文件：[api-endpoint.ts](src/shared/api-endpoint.ts)

```typescript
// OpenAI transport:  baseUrl + "/chat/completions"
// Anthropic transport: baseUrl + "/v1/messages"（已有 /v1 则只加 /messages）
export function resolveApiEndpoint(baseUrl: string, transport: Transport): { url: string }
```

### 1.8 推理/思考控制

每个厂商+模型组合有 `ReasoningCapability`（定义在 `src/shared/reasoning.ts` 的 200+ 行规则表）：

- `control` 类型：`none`、`toggle`、`effort`、`toggle-effort`、`fixed-on`、`dynamic`
- `requestStyle`：如何在线路上编码思考开关（`openai-effort`、`thinking-type`、`anthropic-adaptive`、`qwen-enable-thinking`、`none`）

`applyReasoningPreference()` 在 [reasoning.ts](src/main/orchestrator/vendors/reasoning.ts) 中按解析结果变换请求体。

### 1.9 Tool Choice 策略

关键文件：[tool-choice-policy.ts](src/main/orchestrator/vendors/tool-choice-policy.ts)

厂商级别的 tool_choice 规则：
- DeepSeek + thinking 开启 → **omit**（DeepSeek 拒绝在 thinking 模式下发 tool_choice）
- Kimi + thinking 开启 → **auto**（不支持 named）
- Anthropic transport + thinking → **auto**（extended thinking 只支持 auto/none）
- 其他情况 + must-call → **named**

### 1.10 完整调用流程

1. 用户在设置 UI 选择厂商（如 "DeepSeek"）→ 写入 `ModelSettings`
2. 设置持久化：当前厂商配置存入 `perProvider["DeepSeek（深度求索）"]`，其他厂商配置也保留
3. LLM 调用时：`loadModelSettings()` → 展开为扁平的 `ModelSettings`
4. `createLlmClient().chatNonStream()` 将 `ModelSettings` 转为 `VendorConfig`
5. `getAdapterForConfig(cfg)` 解析 transport → 实例化适配器（缓存）
6. 适配器 `buildRequest()` / `buildStreamRequest()` 将统一 `ChatRequest` 翻译为厂商 wire format
7. `fetch()` 发 HTTP 请求
8. 响应经适配器 `parseResponse()` / `parseStreamEvent()` 解析为统一 `ChatResponse`
9. 调用方完全不感知厂商差异

### 1.11 关键设计模式

- **Adapter Pattern**：`ChatVendorAdapter` 接口 + 两个实现（`OpenAICompatAdapter` / `AnthropicAdapter`）
- **Factory Pattern**：`getAdapter()` / `getAdapterForConfig()`
- **Capability Table**：`PROVIDER_CAPABILITIES` 是唯一事实来源
- **Transport Agnosticism**：调度层绝不出现 `if (provider === "xxx")`
- **Per-Provider Profile Caching**：切换厂商保留所有厂商的设置

---

## 二、Embedding 实现

### 2.1 架构概览

昔涟 Agent 的 Embedding 系统采用**双模式架构**，统一 `EmbeddingProvider` 接口：

```
用户输入 / LLM 响应
        │
        ▼
┌──────────────────┐     ┌───────────────────────┐
│  Scene Embedder  │     │  Sticker Embedder     │
│  (bge-m3 only)   │     │  (bge-m3 or cloud)   │
└────────┬─────────┘     └───────────┬───────────┘
         │                           │
         ▼                           ▼
  Scene Cache               Sticker Cache
         │
         ▼
┌──────────────────┐     ┌───────────────────────┐
│ EmbeddingManager │◄───►│  Embedding Provider   │
│ (download/del)   │     │  local: bge-m3 (1024d)│
└──────────────────┘     │  cloud: OpenAI-compat │
                         └───────────┬───────────┘
                                     │
              ┌──────────────────────┼──────────────────────┐
              │                      │                      │
              ▼                      ▼                      ▼
    ┌─────────────────┐   ┌──────────────────┐   ┌──────────────────┐
    │  JsonVectorStore │   │  Document Cache  │   │  Document Worker │
    │  IVF + cosine    │   │  (SHA-256 keyed) │   │  (worker_thread) │
    │  memory-store    │   │  document-cache  │   │  chunk + embed   │
    └────────┬────────┘   └──────────────────┘   └──────────────────┘
             │
             ▼
    ┌─────────────────┐
    │ HybridRetriever │
    │ Vector(70%) +   │
    │ BM25(30%) +     │
    │ Reranker        │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐     ┌───────────────────┐
    │  RAG Index      │     │  Memory Manager   │
    │  searchMemory   │     │  PMRS L2 sync     │
    │  importDocument │     │  conflict detect  │
    └─────────────────┘     └───────────────────┘
             │
             ▼
    ┌─────────────────┐
    │  Worldbook      │
    │  DMAE keyword   │
    │  activation     │
    └─────────────────┘
```

### 2.2 核心接口

关键文件：[rag/embedding.ts](src/main/rag/embedding.ts)

```typescript
export interface EmbeddingProvider {
  embed(text: string): Promise<number[]>;
  embedBatch(texts: string[]): Promise<number[][]>;
  readonly dims: number;
  readonly name: string;
  readonly cacheIdentity?: EmbeddingProviderIdentity;
  readonly workerConfig?: EmbeddingWorkerConfig;
  readonly declaredDimensions?: number;
  readonly resolvedDimensions?: number;
}
```

### 2.3 两种 Provider

#### 2.3.1 本地 Provider（bge-m3 via ONNX）

```typescript
// 模型：Xenova/bge-m3，1024 维
const LOCAL_MODELS = {
  bgem3: { key: "bgem3", hfName: "Xenova/bge-m3", dims: 1024 },
};

// 加载 pipeline（仅一次，缓存复用）
const { pipeline, env } = await importEsm("@xenova/transformers");
env.allowLocalModels = true;
env.allowRemoteModels = false;
env.localModelPath = getProjectModelsDir(); // 项目根 models/
const pipe = await pipeline("feature-extraction", "Xenova/bge-m3", {
  cache_dir: path.join(os.homedir(), ".cache", "huggingface"),
});

// 推理
const result = await pipe(text, { pooling: "mean", normalize: true });
return Array.from(result.data); // Float32Array → number[]
```

模型文件存储位置：
- 主路径：项目根 `models/bge-reranker-base/`、`models/ms-marco-MiniLM-L-6-v2/`
- 兜底路径：`~/.cache/huggingface/Xenova/bge-m3/onnx/model_quantized.onnx`

`@xenova/transformers` 通过 ESM 动态 import 加载（`new Function("moduleName", "return import(moduleName)")`）。

#### 2.3.2 云端 Provider（OpenAI 兼容）

```typescript
export function createOpenAIEmbeddingProvider(
  baseUrl: string,
  apiKey: string,
  model = "text-embedding-ada-002",
  declaredDimensions?: number,
): EmbeddingProvider {
  return {
    async embed(text: string): Promise<number[]> {
      const res = await fetch(baseUrl + "/embeddings", {
        headers: { Authorization: "Bearer " + apiKey },
        body: JSON.stringify({ model, input: text }),
      });
      const data = await res.json();
      return data.data[0].embedding;
    },
    // ...embedBatch, dims, cacheIdentity, workerConfig
  };
}
```

特点：
- 任何 OpenAI 兼容的 `/embeddings` 端点均可用
- 首次调用自动探测维度
- 每次后续调用校验维度一致性（`EmbeddingDimensionMismatchError`）

#### 2.3.3 自动选择策略

```typescript
export function getEmbeddingProvider(
  mode: "auto" | "local" | "cloud" = "auto",
  cloudBaseUrl?: string,
  cloudApiKey?: string,
  modelKey?: string,
  cloudDimensions?: number,
): EmbeddingProvider | null {
  if (mode === "local") return createLocalEmbeddingProvider(modelKey);
  if (mode === "cloud" && cloudBaseUrl && cloudApiKey)
    return createOpenAIEmbeddingProvider(cloudBaseUrl, cloudApiKey, modelKey, cloudDimensions);

  // auto 模式：优先 local → 本地不可用且有云配置 → cloud → null
  const local = createLocalEmbeddingProvider(modelKey);
  if (local) return local;
  if (cloudBaseUrl && cloudApiKey)
    return createOpenAIEmbeddingProvider(cloudBaseUrl, cloudApiKey, modelKey, cloudDimensions);
  return null;
}
```

### 2.4 五个应用场景

#### 2.4.1 RAG 向量存储（JsonVectorStore）

关键文件：[rag/vectorstore.ts](src/main/rag/vectorstore.ts)

存储位置：`{userData}/rag-data/memory-store.json`

数据结构：

```typescript
interface MemoryEntry {
  id: string;
  text: string;
  embedding: number[];        // 1024 维向量
  source: string;             // "user_memory" | "worldbook" | "imported_doc" | "chat_history"
  weight: number;             // 从 1.0 开始，添加+0.1，召回+0.05，衰减
  createdAt: number;
  lastRecalledAt: number;
  metadata?: Record<string, unknown>;
}
```

搜索算法 — IVF 加速余弦相似度：
- **IVF 索引**：k-means 聚类，K ≈ √n/2（最大 512 簇）
- **nprobe** = K/8 个簇被搜索
- **评分**：`cosine_similarity(query_vec, entry_vec) × entry.weight × decayFactor`
- **衰减**：每 24 小时未召回衰减 5%
- **元数据**：记录 `{ provider, model, dimensions, cacheIdentity }` 用于跨 provider 一致性检查

#### 2.4.2 混合检索（HybridRetriever）

关键文件：[rag/retriever.ts](src/main/rag/retriever.ts)

三种检索方法融合：

| 方法 | 权重 | 说明 |
|---|---|---|
| **Vector 搜索** | 70% | IVF 加速余弦相似度 |
| **BM25 关键词** | 30% | 自定义 BM25 + `@node-rs/jieba` 中文分词（名词权重×1.3、停用词衰减×0.3） |
| **Reranker** | 可选 | `bge-reranker-base` 交叉编码器对 top-K 重排序 |

#### 2.4.3 记忆系统（PMRS — Personal Memory Retrieval System）

关键文件：
- [memory/memory-store.ts](src/main/memory/memory-store.ts) — 三层记忆存储
- [memory/memory-manager.ts](src/main/memory/memory-manager.ts) — 写入编排

三层架构：

| 层 | 内容 | 向量化 | 存储 |
|---|---|---|---|
| **L0**（核心档案） | 昵称、称谓、职业、兴趣、语言 | ❌ | `memory.json` |
| **L1**（近期上下文） | 近期目标、偏好、当前项目 | ❌ | `memory.json` |
| **L2**（长期情景记忆） | 单个记忆（内容+触发词+来源引用） | ✅ | `memory.json` + RAG 向量库 |

L2 写入流程：
1. `MemoryManager.writeL2()` → 写入 `memory.json`
2. `addL2MemoryVector(content, l2Id, metadata)` → 嵌入内容 → 加入 `JsonVectorStore`
3. 更新同步状态：`pending_sync` → `synced`

**记忆冲突检测**：用向量相似度搜索找到语义相近的已有记忆，再用本地词汇矛盾规则判断。

#### 2.4.4 场景识别（Scene Embedder）

关键文件：[scene-embedder.ts](src/main/scene-embedder.ts)

- 42 个例句 × 7 个场景（日常、问候、安慰、被夸、俏皮、告别、关心）
- 启动时嵌入所有场景例句
- 每条用户消息嵌入后与所有场景向量计算余弦相似度
- **加权向量**：最近 3 条用户消息分别嵌入，权重 0.75 / 0.20 / 0.05 组合
- 检测的场景用于注入语调 prompt 到 LLM 上下文
- **缓存**：`{userData}/scene-embedding-cache.json`（按 provider identity 做 key）
- **固定 bge-m3**：不受 RAG 模型切换影响

#### 2.4.5 贴纸匹配（Sticker Embedder）

关键文件：[sticker-embedder.ts](src/main/sticker-embedder.ts)

- 内置贴纸描述 + 用户上传贴纸全部嵌入
- LLM 回复嵌入后与贴纸索引匹配，自动推荐表情
- **缓存**：`{userData}/sticker-embedding-cache.json`
- **缓存 key** 包含 provider identity + 贴纸描述 → 换贴纸自动失效

### 2.5 文档导入

关键文件：[rag/document-index-worker.ts](src/main/rag/document-index-worker.ts)

- **Worker 线程**执行，避免阻塞主线程
- 文件类型检测 → 二进制拒绝 → 文本提取
- **分块**：[rag/chunk.ts](src/main/rag/chunk.ts) — 滑动窗口 512 tokens / overlap 128，支持 Markdown 标题前缀
- **内容寻址缓存**：[rag/document-cache.ts](src/main/rag/document-cache.ts) — SHA-256 去重，避免重复嵌入
- 支持取消操作

### 2.6 模型管理

关键文件：[embedding-manager.ts](src/main/embedding-manager.ts)

- 下载模型：从 HuggingFace（或 `hf-mirror.com` 镜像）
- 状态检查：确认 `tokenizer.json`、`config.json`、`onnx/model_quantized.onnx` 三个文件存在
- 删除模型：清理 `~/.cache/huggingface/Xenova/bge-m3/` 目录
- 多路径探测：`CYRENE_MODELS_DIR` 环境变量 → `{cwd}/models/` → `{appPath}/models/` → `{resourcesPath}/models/`

### 2.7 Embedding 服务层

关键文件：[services/embedding/embedding-index-service.ts](src/main/services/embedding/embedding-index-service.ts)

`EmbeddingIndexService` 管理两个嵌入索引的生命周期：

- **贴纸嵌入索引**：贴纸变化或模型切换时刷新
- **场景嵌入索引**：固定 bge-m3，独立于 RAG 模型

两者均在启动时异步构建（延迟 1500ms 避免阻塞启动）。

### 2.8 配置项

在 `ModelSettings` 中：

| 配置 | 默认值 | 说明 |
|---|---|---|
| `embeddingModel` | `"bgem3"` | 目前唯一支持的模型 |
| `embeddingDimensions` | 可选 | 云端 provider 的维度声明 |
| `ragMode` | `"auto"` | `"auto"` / `"local"` / `"cloud"` |

热切换模型：`switchEmbeddingModel()` → 清理旧 pipeline → 检查维度匹配 → 重建 retriever。

---

## 三、天气获取工具

### 3.1 架构概览

```
用户问 "今天北京天气怎样"
        │
        ▼
    LLM 判断 → 调用 weather 工具
        │
        ▼
    executeWeather(args)
        ├─ 确定城市: args.city → 用户默认城市 → 提示设置
        ├─ 确定天气源: 用户设置 → 默认 "open-meteo"
        ├─ open-meteo 分支:
        │   ├─ omResolveCity(city) → Geocoding API → {lat, lng, name, admin1}
        │   └─ omFetchWeather(city) → Forecast API → 实况 + 预报
        │       ├─ temperature_2m / apparent_temperature / humidity
        │       ├─ precipitation / weather_code / wind_speed / wind_direction
        │       ├─ surface_pressure / uv_index / visibility
        │       └─ WMO 代码→中文 (0=晴, 61=小雨, 95=雷暴...)
        └─ amap 分支:
            ├─ amapResolveAdcode(city) → 行政区查询 → adcode
            └─ amapFetchWeather(city) → weatherInfo API
                └─ weather / temperature / humidity / winddirection / windpower / reporttime
        │
        ▼
    返回 JSON + 发送 WeatherCardData 到渲染端 → WeatherCard 组件渲染
```

### 3.2 双天气源

| 天气源 | API 地址 | 是否需要 Key | 数据特点 |
|---|---|---|---|
| **Open-Meteo**（默认） | `api.open-meteo.com` | 免费免配置 | 国际数据，WMO 天气代码 |
| **高德天气** | `restapi.amap.com/v3/weather` | 需要高德 Web API Key | 国内数据更准，中文天气描述 |

### 3.3 工具注册

关键文件：[orchestrator/built-in-tools.ts](src/main/orchestrator/built-in-tools.ts#L1000-L1044)

```typescript
toolRegistry.register({
  id: "weather",
  name: "查天气",
  description:
    "查询指定城市的实时天气。返回温度、体感温度、湿度、风速风向、降水、日出日落、AQI、UV 等。\n\n" +
    "何时用：\n" +
    "- 用户问'今天天气怎样''外面冷不冷''热不热''要不要带伞''穿什么'\n" +
    "- 用户提到城市名 + 天气相关词\n" +
    "- 用户问'周末适合出去玩吗'且涉及天气判断\n\n" +
    "不要用于：\n" +
    "- 历史天气（'上周北京天气'）—— 做不到，直接告诉用户\n" +
    "- 逐小时精确预报\n" +
    "- 完全跟天气无关的问题\n\n" +
    "参数：city（可选，城市名中文或拼音；不传则用用户设置的默认城市）。",
  enabled: true,
  risk: "network",
  effectKind: "read",
  verificationPolicy: "none",
  inputSchema: {
    type: "object",
    properties: {
      city: { type: "string", description: "要查询的城市名（中文或拼音），不传则用用户默认城市" },
    },
    required: [],
  },
  soulActionLabel: "查询天气",
  soulProjection: {
    projector: "entity_detail",
    source: "trusted_internal",
    fields: { title: "city", region: "region", weather: "weather", ... },
  },
  completionEvidence: [{ kind: "tool_succeeded" }],
  execute: executeWeather,
});
```

### 3.4 Open-Meteo 实现

关键代码：[built-in-tools.ts](src/main/orchestrator/built-in-tools.ts#L746-L874)

两步流程：

#### Step 1: 地理编码
```typescript
async function omResolveCity(city: string): Promise<OMCity | null> {
  const url = `https://geocoding-api.open-meteo.com/v1/search?name=${encodeURIComponent(city)}&count=1&language=${getWeatherLanguage()}&format=json`;
  // → 返回 { name, latitude, longitude, country, admin1 }
}
```

#### Step 2: 天气查询
```typescript
async function omFetchWeather(city: string): Promise<string> {
  const loc = await omResolveCity(city);
  const url = `https://api.open-meteo.com/v1/forecast?latitude=${loc.latitude}&longitude=${loc.longitude}&current=${currentParams}&daily=${dailyParams}&timezone=auto`;
  // currentParams: temperature_2m, relative_humidity_2m, apparent_temperature,
  //   precipitation, weather_code, wind_speed_10m, wind_direction_10m,
  //   surface_pressure, uv_index, visibility
  // dailyParams: temperature_2m_max/min, weather_code, wind_speed_10m_max, wind_direction_10m_dominant
}
```

#### WMO 天气代码→中文

```typescript
function omWeatherCodeText(code: number): string {
  // 0→晴, 1→晴间多云, 2→多云, 3→阴
  // 45→雾, 51/61→小雨, 53/63→中雨, 55/65→大雨
  // 71→小雪, 73→中雪, 75→大雪
  // 80→阵雨, 82→暴雨
  // 95→雷暴, 96→雷暴伴冰雹, 99→强雷暴伴冰雹
}
```

#### 风向角度→中文方位

```typescript
function omWindDir(deg: number): string {
  // 0°=北, 45°=东北, 90°=东, ..., 每22.5°一个方位，共16个
  const dirs = ["北", "东北偏北", "东北", "东北偏东", "东", "东南偏东", "东南", "东南偏南",
    "南", "西南偏南", "西南", "西南偏西", "西", "西北偏西", "西北", "西北偏北"];
  return dirs[Math.round(deg / 22.5) % 16];
}
```

#### Open-Meteo 返回格式

```json
{
  "city": "北京",
  "region": "北京",
  "weather": "晴",
  "temperature": 22.5,
  "feelsLike": 21.0,
  "humidity": 45,
  "windDirection": "北风",
  "windSpeed": "15km/h",
  "precipitation": 0.0,
  "pressure": 1013,
  "uv": 3,
  "visibility": 10,
  "source": "Open-Meteo",
  "updateTime": "14:30"
}
```

### 3.5 高德天气实现

关键代码：[built-in-tools.ts](src/main/orchestrator/built-in-tools.ts#L876-L954)

两步流程：

#### Step 1: 行政区查询
```typescript
async function amapResolveAdcode(city: string, key: string): Promise<AmapDistrict | null> {
  const url = `https://restapi.amap.com/v3/config/district?keywords=${encodeURIComponent(city)}&subdistrict=0&key=${key}`;
  // → 返回 { adcode, name, level }
}
```

#### Step 2: 天气查询
```typescript
async function amapFetchWeather(city: string, key: string): Promise<string> {
  const district = await amapResolveAdcode(city, key);
  const url = `https://restapi.amap.com/v3/weather/weatherInfo?city=${district.adcode}&key=${key}&extensions=base`;
  // → 返回 lives[]: { province, city, weather, temperature, winddirection, windpower, humidity, reporttime }
}
```

#### 高德返回格式

```json
{
  "city": "北京",
  "region": "北京",
  "weather": "多云",
  "temperature": 22,
  "humidity": 45,
  "windDirection": "东南",
  "windSpeed": "3级",
  "source": "高德天气",
  "updateTime": "14:30"
}
```

### 3.6 主执行函数

```typescript
async function executeWeather(args: Record<string, unknown>): Promise<string> {
  // 1. 检查功能是否启用
  if (weatherEnabledGetter && !weatherEnabledGetter()) {
    return "[错误] 天气查询功能未启用，请在设置里开启";
  }

  const source = weatherSourceGetter?.() ?? "open-meteo";

  // 2. 确定城市：参数优先 → 用户默认城市 → 提示
  let city = String(args.city ?? "").trim();
  if (!city) city = (weatherCityGetter?.() ?? "").trim();
  if (!city) return "[提示] 没有指定城市，也没设置默认城市...";

  // 3. 按天气源分支
  if (source === "open-meteo") return omFetchWeather(city);
  if (source === "amap") {
    const amapKey = amapKeyGetter?.() ?? "";
    if (!amapKey) return "[错误] 还没有配置高德天气 Key...";
    return amapFetchWeather(city, amapKey);
  }
  return `[错误] 未知的天气源"${source}"...`;
}
```

### 3.7 天气卡片 UI

除了返回 JSON 文本给 LLM，天气工具还通过 `weatherCardCallback` 发送结构化 `WeatherCardData` 给渲染端：

```typescript
export interface WeatherCardData {
  source: "open-meteo" | "amap";
  location: { province: string; city: string };
  // Open-Meteo 字段
  weatherCode?: number;
  temp: number;
  feelsLike?: number;
  humidity: number;
  windDeg?: number;
  windSpeed?: number;
  precipitation?: number;
  pressure?: number;
  // 高德字段
  weather?: string;
  windDirection?: string;
  windPower?: string;
  reporttime?: string;
}
```

渲染端文件：
- [weather-types.ts](src/renderer/react/features/chat/components/weather/weather-types.ts) — TypeScript 类型定义（`WeatherCategory`: clear/cloudy/rain/snow/thunder）
- [weather-utils.ts](src/renderer/react/features/chat/components/weather/weather-utils.ts) — WMO/高德代码映射、风向转换、日期格式化
- **WeatherCard.tsx** — 主卡片组件，按 Open-Meteo / 高德分别渲染
- **WeatherIllustration.tsx** — SVG/CSS 天气插画（太阳、云、雨、雪、闪电）
- **weather-card.css** — 完整样式（明暗主题、动画、响应式布局）

### 3.8 配置注入

关键文件：[startup/bootstrap-config.ts](src/main/startup/bootstrap-config.ts)

```typescript
setWeatherConfig(
  cityGetter,        // → loadUserProfile().defaultCity
  sourceGetter,      // → loadGeneralSettings().weatherSource
  amapKeyFn,         // → loadGeneralSettings().amapKey
  cardCb,            // → IPC 发送 "cyrene.weather" CUSTOM 事件到渲染端
  enabledGetter,     // → loadGeneralSettings().weatherEnabled
);
```

设计为启动时注入 getter 函数（而非直接传值），在工具执行时才读取最新配置值，同时避免循环依赖（工具模块不 import `index.ts` 或 settings store）。

### 3.9 地域支持

关键文件：[locale-context.ts](src/main/locale-context.ts)

`getWeatherLanguage()` 将用户 UI 语言映射为天气 API 语言代码：
- 中文 → `"zh"`
- 英文 → `"en"`
- 日文 → `"ja"`
- 韩文 → `"ko"`

作为 Open-Meteo Geocoding API 的 `language` 参数。

### 3.10 超时与错误处理

所有网络请求统一 15 秒超时（`WEATHER_TIMEOUT_MS = 15000`），使用 `AbortController` + `setTimeout` 实现。

错误处理矩阵：

| 场景 | 返回 |
|---|---|
| 功能未启用 | `[错误] 天气查询功能未启用，请在设置里开启` |
| 无城市且无默认值 | `[提示] 没有指定城市，也没设置默认城市...` |
| 城市无法解析 | `[错误] 找不到城市"xxx"...` |
| 高德源未配置 Key | `[错误] 还没有配置高德天气 Key...` |
| 未知天气源 | `[错误] 未知的天气源"xxx"...` |
| 网络异常 | `[错误] 天气查询失败：{message}` |
| 超时 | 自动 Abort → 网络异常分支 |

---

## 四、总结

昔涟 Agent 的架构在三方面都体现了**厂商无关、可插拔**的设计理念：

| 方面 | 核心模式 | 关键接口 | 支持范围 |
|---|---|---|---|
| **LLM 切换** | 适配器模式 + 能力表 | `ChatVendorAdapter` | 9 家厂商，2 种 transport |
| **Embedding** | Provider 抽象 | `EmbeddingProvider` | 本地 ONNX + 云端 API，5 个应用场景 |
| **天气工具** | 双源策略 + 配置注入 | `toolRegistry.register()` | Open-Meteo（免费）+ 高德（国内精准） |

三者共同构成：
- **工具系统**统一走 `toolRegistry`，不感知 LLM 厂商
- **LLM 客户端**只看 `VendorConfig` + adapter，不关心具体协议
- **Embedding** 通过 `EmbeddingProvider` 接口抽象本地/云端差异
- 所有配置通过 **getter 注入** 而非直接 import，避免循环依赖并支持热切换
