# 🎭 MutiRoleAgent（多角色人设AI助手）任务板

> 基于 EchoBot / Cyrene-Agent 对比分析，取长补短、分阶段迭代。
> Live2D 暂不考虑，前端保持 Streamlit。

---

## 📊 三项目现状对比

| 模块 | EchoBot (Python) | Cyrene-Agent (TS) | **MutiRoleAgent (我们)** |
|------|:--:|:--:|:--:|
| Agent 核心 | LangGraph ReAct | LangGraph ReAct | LangGraph ReAct ✅ |
| 决策路由 | ✅ DecisionEngine (Chat/Agent) | ✅ TaskRouter | ⚠️ CITA 仅意图分类 |
| 上下文管理 | 内联裁剪 | ✅ CITA (3 层管理) | ⚠️ 简单裁剪 |
| 角色人设 | ✅ RoleplayEngine | ✅ Soul + Styles + Worldbook | ⚠️ YAML Prompt 覆盖 |
| Skill 系统 | ✅ skill_support | ✅ skills/ (TS) | ✅ skill_support (12个) |
| 子 Agent | ❌ | ✅ sub-agents | ❌ |
| 结构化输出 | ❌ | ✅ StructuredOutput | ❌ |
| 记忆系统 | 内存 + 文件 | ContextStore + Social | SQLite + ChromaDB ✅ |
| 调度系统 | ✅ Cron + Heartbeat | ⚠️ Social scheduler | ❌ |
| 渠道抽象 | ✅ QQ/Telegram/Console | ✅ WeChat/Feishu/Web | ❌ 仅 Streamlit |
| TTS/ASR | ✅ 多引擎 | ✅ 5 TTS 引擎 | ❌ |
| Live2D | ✅ 内置模型 | ✅ Cyrene 角色 | 🔜 计划中 |
| 测试 | ✅ 20+ 文件 | ✅ 单元+集成 | ⚠️ 少量 |

---

## 🎯 优化路线图

```
Phase 1 ──→ Phase 2 ──→ Phase 3 ──→ Phase 4
Prompt架构   Decision    CITA 2.0    Persona
升级          Engine      上下文管理   角色系统升级
(Cyrene)      (EchoBot)    (Cyrene)    (Cyrene+EchoBot)

Phase 5 ──→ Phase 6 ──→ Phase 7 ──→ Phase 8
子Agent       结构化输出   调度系统     多层记忆升级
(Cyrene)      (Cyrene)    (EchoBot)    (两者融合)

Phase 9 ──→ Phase 10 ──→ Phase 11
渠道抽象层     测试体系     后端服务化
(EchoBot)                 (FastAPI)
```

---

## 📋 Phase 1：Prompt 架构升级

> **借鉴**：Cyrene-Agent 的 `prompts/` 目录结构（soul.md + styles/ + worldbook/）
> **目标**：把当前混在 YAML 里的人设/语气/世界观拆成独立文件，易维护、可组合

### 1.1 拆分 Prompt 结构
```
prompts/
├── soul/                       ← 角色灵魂（核心人设，不动）
│   ├── cyrene.md               ← 昔涟
│   ├── columbina.md            ← 哥伦比亚
│   ├── ye-shunguang.md         ← 叶瞬光
│   └── zhuang-fangyi.md        ← 庄方宜
├── styles/                     ← 语气风格（可切换、可组合）
│   ├── default.md              ← 默认
│   ├── lively.md               ← 活泼
│   ├── healing.md              ← 治愈
│   └── serious.md              ← 认真
├── worldbook/                  ← 世界观设定（共享知识）
│   ├── world.md                ← 世界观
│   ├── characters.md           ← 角色关系
│   └── glossary.md             ← 术语表
├── system/                     ← 系统级 Prompt
│   ├── tools.md                ← 工具使用规则
│   ├── safety.md               ← 安全约束
│   └── output.md               ← 输出格式要求
└── composer.py                 ← Prompt 组合器
```

### 1.2 任务清单
- [ ] 创建 `prompts/soul/` — 每个角色一个 .md，纯 Markdown 写法
- [ ] 创建 `prompts/styles/` — 3~5 种语气风格
- [ ] 创建 `prompts/worldbook/` — 世界观 + 角色关系 + 术语
- [ ] 创建 `prompts/system/` — 工具规则 + 安全约束 + 输出格式
- [ ] 实现 `prompts/composer.py` — 按 (`role` + `style` + `context`) 动态拼接 Prompt
- [ ] 适配 `react_agent.py` — 从 YAML 加载切换到 composer
- [ ] 适配 `persona_loader.py` — 支持 soul + style 组合加载
- [ ] 保留旧 YAML 兼容（渐进迁移）

**✅ 交付物**：每个角色的 Prompt 可独立编辑、风格可切换、世界观共享

---

## 📋 Phase 2：Decision Engine（快/慢路由）

> **借鉴**：EchoBot 的 `DecisionEngine`（Chat vs Agent 二分类）
> **目标**：纯闲聊不加载工具，节省 Token 和延迟

### 2.1 架构设计
```
用户消息
    │
    ▼
┌──────────────────┐
│ Decision Engine  │  ← 轻量分类器（规则 + 可选快模型）
│                  │
│  "chat" ─────────┼──→ 直接 Roleplay 回复（无工具、低延迟）
│  "agent" ────────┼──→ 加载工具 → ReAct Agent Loop
└──────────────────┘
```

### 2.2 任务清单
- [ ] 创建 `agent/decision_engine.py`
  - [ ] 规则匹配层（关键词 + 正则，覆盖 80% 场景）
  - [ ] 可选 LLM 层（快模型做二分类，处理规则覆盖不到的）
  - [ ] 决策缓存（同话题连续 chat 不重复判断）
- [ ] 定义触发 Agent 的规则
  - [ ] 工具调用关键词（天气、搜索、下载、文件、推荐……）
  - [ ] Skill 名称提及
  - [ ] 追问/继续/重试类指令
- [ ] 与现有 CITA 协同——CITA 在 agent 路径下做细粒度意图分类
- [ ] 在 `react_agent.py` 集成——chat 路径直接调 LLM 回复，不走工具加载
- [ ] 监控指标——chat/agent 比例、Token 节省量

**✅ 交付物**："你好"不加载工具秒回，"推荐番剧"走完整 Agent 流程

---

## 📋 Phase 3：CITA 2.0 上下文管理

> **借鉴**：Cyrene-Agent 的 CITA 系统（Semantic Engine + Structural Reducer + Token Budget）
> **现状**：`cita_classifier.py` 仅做 5 分类（ANIME/WEATHER/FILE/REPORT/CHAT）

### 3.1 升级架构
```
原 CITA (1.0):            升级后 CITA (2.0):
                         
用户消息                  用户消息 + 历史
    │                        │
    ▼                        ▼
┌──────────┐           ┌──────────────┐
│ 5 分类    │           │ Semantic     │ ← 提取关键信息（意图/实体/情绪）
│ 意图识别  │           │ Engine       │
└──────────┘           └──────┬───────┘
                              │
                         ┌────▼───────┐
                         │ Structural  │ ← 上下文压缩（去重/摘要/裁剪）
                         │ Reducer     │
                         └──────┬──────┘
                                │
                         ┌──────▼───────┐
                         │ Token        │ ← 预算分配（System/Persona/History/Skill）
                         │ Budget       │
                         └──────────────┘
```

### 3.2 任务清单
- [ ] 重构 `agent/cita_classifier.py` → `agent/cita/`
  - [ ] `semantic.py` — 实体提取（番名/人名/地名/时间）、情绪识别
  - [ ] `reducer.py` — 上下文去重 + 无关信息裁剪
  - [ ] `budget.py` — Token 预算分配（按优先级配给 System/Persona/History/Skill）
- [ ] Token 预算策略
  - [ ] System Prompt: 最多 30%
  - [ ] Persona Overlay: 最多 15%
  - [ ] 对话历史: 最多 35%
  - [ ] Skill 指令: 最多 20%
- [ ] 与 `context_trimmer.py` 协同——CITA 管"分多少"，Trimmer 管"裁哪些"
- [ ] 可视化管理——Streamlit 侧边栏显示当前 Token 分布

**✅ 交付物**：长对话不爆 Token，关键信息不丢失，每部分有明确预算

---

## 📋 Phase 4：Persona 角色系统升级

> **借鉴**：EchoBot 的 `RoleplayEngine` + Cyrene-Agent 的 `Soul + Styles`
> **目标**：从"Prompt 覆盖"升级为"角色引擎"

### 4.1 升级架构
```
用户输入 + 对话上下文
        │
        ▼
┌───────────────────┐
│ Persona Engine    │
│                   │
│ ┌───────────────┐ │
│ │ Soul Loader   │ │ ← 加载角色灵魂文件（soul/*.md）
│ └───────────────┘ │
│ ┌───────────────┐ │
│ │ Style Picker  │ │ ← 按场景/情绪选语气风格（styles/*.md）
│ └───────────────┘ │
│ ┌───────────────┐ │
│ │ Worldbook     │ │ ← 注入相关世界观知识
│ │ Retriever     │ │
│ └───────────────┘ │
│ ┌───────────────┐ │
│ │ Tone Injector │ │ ← 按角色 + 风格 + 情绪 → 生成语气指令
│ └───────────────┘ │
└─────────┬─────────┘
          │
          ▼
   最终 System Prompt
```

### 4.2 任务清单
- [ ] 创建 `agent/persona/` 包
  - [ ] `engine.py` — Persona Engine 主控
  - [ ] `soul_loader.py` — 加载灵魂文件 + 缓存
  - [ ] `style_picker.py` — 按场景/情绪自动选风格
  - [ ] `worldbook_retriever.py` — RAG 检索相关世界观知识
  - [ ] `tone_injector.py` — 生成语气指令
- [ ] 角色情绪感知
  - [ ] 用户情绪检测（从 CITA semantic engine 获取）
  - [ ] 角色情绪映射（用户愤怒 → 角色温柔安抚；用户开心 → 角色活泼）
- [ ] 风格切换
  - [ ] 用户手动切换（Streamlit 下拉框）
  - [ ] 自动适配（根据上下文和话题）
- [ ] 角色间切换时的上下文处理
  - [ ] 告知用户角色已切换
  - [ ] 保留核心对话记忆，切换角色语气

**✅ 交付物**：4 角色 × 4 风格 = 16 种语气组合，情绪自适应

---

## 📋 Phase 5：子 Agent 系统

> **借鉴**：Cyrene-Agent 的 `sub-agents/`（document-agent, search-agent）
> **目标**：复杂任务自动分解为子 Agent 并行执行

### 5.1 场景示例
```
用户："帮我下载《无职转生》小说，然后推荐几部类似的异世界番，最后把结果导出为报告"

主 Agent 分解：
┌─────────────────┐
│ Task Planner    │
│                 │
│ ├─ 子任务1: 下载小说     → download-novel agent
│ ├─ 子任务2: 推荐类似番   → recommend-anime agent
│ └─ 子任务3: 导出报告     → file-processor agent
└─────────────────┘
        │
        ▼
  并行执行 → 结果合并 → 统一回复
```

### 5.2 任务清单
- [ ] 创建 `agent/sub_agents/` 包
  - [ ] `planner.py` — 任务分解（复杂任务 → 子任务列表）
  - [ ] `runner.py` — 并行执行子任务
  - [ ] `merger.py` — 结果合并 + 去重 + 格式化
  - [ ] `types.py` — 子任务数据结构
- [ ] 每个 Skill 可声明 "可作为子 Agent 运行"
- [ ] 主 Agent 判断是否需要用子 Agent（任务复杂度评估）
- [ ] 子 Agent 间通信（共享中间结果）
- [ ] 超时 + 降级（某个子 Agent 失败不影响其他）

**✅ 交付物**：复杂请求自动并行处理，响应时间显著缩短

---

## 📋 Phase 6：结构化输出

> **借鉴**：Cyrene-Agent 的 `structured-output/`（JSON Schema + 自动重试）
> **目标**：关键输出有格式保证，减少"说了但没完全说"的情况

### 6.1 输出约束场景
- 动漫推荐 → 固定格式（番名/评分/类型/简介）
- 天气查询 → 结构化数据（温度/湿度/风力/建议）
- 季度新番 → 表格格式
- 报告生成 → Markdown 模板

### 6.2 任务清单
- [ ] 创建 `agent/structured_output/`
  - [ ] `schemas.py` — 各类输出的 JSON Schema 定义
  - [ ] `validator.py` — 输出格式校验
  - [ ] `retry.py` — 格式不符时自动重试（最多 1 次）
  - [ ] `formatter.py` — Schema 转自然语言展示
- [ ] 为每个 Skill 定义输出 Schema（在 SKILL.md metadata 中声明）
- [ ] Streamlit 前端适配——结构化数据渲染为卡片/表格

**✅ 交付物**：推荐结果统一格式，不会每次回复结构不同

---

## 📋 Phase 7：调度系统

> **借鉴**：EchoBot 的 `scheduling/`（Cron + Heartbeat）
> **目标**：支持定时任务和周期性推送

### 7.1 应用场景
- "每天早上 8 点给我发今天的天气"
- "每周五推荐周末可以追的番"
- "新番更新时提醒我"
- "每隔 1 小时检查一次 XX 状态"

### 7.2 任务清单
- [ ] 创建 `agent/scheduling/`
  - [ ] `cron_parser.py` — 自然语言 → Cron 表达式
  - [ ] `scheduler.py` — 定时任务管理（注册/执行/取消）
  - [ ] `job_store.py` — 任务持久化（SQLite）
  - [ ] `executor.py` — 到时间自动执行 + 结果推送
- [ ] Streamlit 定时任务管理面板
- [ ] 失败重试 + 超时处理
- [ ] 心跳检测（检测 Agent 是否在线）

**✅ 交付物**：用户可以设提醒、定时推送，Agent 变"主动"

---

## 📋 Phase 8：多层记忆系统升级

> **融合**：EchoBot 的 memory/ + Cyrene-Agent 的 social-context/
> **现状**：SQLite 全量 + ChromaDB RAG，缺摘要和社交记忆

### 8.1 升级架构
```
Layer 1: 全量存储 ─── 所有消息写入 SQLite（已有 ✅）
Layer 2: 滚动摘要 ─── 每 30 轮自动压缩为摘要（🆕）
Layer 3: 长期记忆 ─── 用户画像 + 偏好 + 事实 RAG（升级 ⚠️）
Layer 4: 社交记忆 ─── 上下文感知 + 关系追踪（🆕）
```

### 8.2 任务清单
- [ ] Layer 2: 滚动摘要
  - [ ] `agent/memory/summarizer.py` — 自动触发摘要生成
  - [ ] 摘要存储 + 注入策略
- [ ] Layer 3: 长期记忆增强
  - [ ] 用户画像提取升级（情感/偏好/习惯/重要日期）
  - [ ] 记忆过期策略（不活跃的记忆降低权重）
  - [ ] 记忆冲突处理（用户说"我不喜欢热血番了"→ 更新偏好）
- [ ] Layer 4: 社交记忆（🆕）
  - [ ] `agent/memory/social_context.py`
  - [ ] 对话频次追踪、话题偏好、情绪变化
- [ ] 记忆优先级：长期画像 > 历史摘要 > 最新对话

**✅ 交付物**：聊 100 轮不失忆，记住你喜欢什么、讨厌什么、上次聊到哪

---

## 📋 Phase 9：渠道抽象层

> **借鉴**：EchoBot 的 `channels/`（统一消息格式 + 多渠道适配）
> **目标**：为未来扩展 QQ/微信/Telegram 做准备

### 9.1 任务清单
- [ ] 创建 `agent/channels/`
  - [ ] `base.py` — 抽象渠道接口（send / receive / format）
  - [ ] `streamlit_adapter.py` — 当前 Streamlit 适配器
  - [ ] `types.py` — 统一消息格式（文本/图片/附件/事件）
- [ ] 重构 `app.py` — 通过 Channel 接口收发消息
- [ ] 预留接口：`qq_adapter.py`, `wechat_adapter.py`, `telegram_adapter.py`
- [ ] （可选）实现 Console 渠道用于调试

**✅ 交付物**：前端实现与 Agent 逻辑解耦，后续换框架不伤核心

---

## 📋 Phase 10：测试体系

> **借鉴**：EchoBot 的 20+ 测试文件
> **目标**：核心模块有覆盖、重构有底气

### 10.1 任务清单
- [ ] 单元测试
  - [ ] Skill 系统（加载/匹配/注册/调用）
  - [ ] Decision Engine（规则命中率）
  - [ ] CITA（意图分类准确率）
  - [ ] Action Gate + Execution Policy（安全拦截）
  - [ ] Context Trimmer（裁剪正确性）
- [ ] 集成测试
  - [ ] 完整对话流程（闲聊/推荐/天气/下载）
  - [ ] Skill 触发准确率（用 skill-creator 的 evals 格式）
  - [ ] 多角色切换
- [ ] E2E 测试
  - [ ] Streamlit 页面渲染
  - [ ] 流式输出完整性

**✅ 交付物**：`pytest` 一键跑通，CI 可接入

---

## 📋 Phase 11：后端服务化

> **目标**：从 Streamlit 单体 → FastAPI + Streamlit 前后分离

### 11.1 任务清单
- [ ] 创建 FastAPI 后端
  - [ ] `POST /chat` — 发送消息（SSE 流式返回）
  - [ ] `POST /session/new` — 新建会话
  - [ ] `GET /session/{id}/history` — 获取历史
  - [ ] `GET /skills` — 列出 Skill
  - [ ] `POST /skills/{name}/invoke` — 调用 Skill
- [ ] Streamlit 改为 API 客户端（或独立共存）
- [ ] 会话管理 API（CRUD）
- [ ] Skill 热加载 API

**✅ 交付物**：一个 `uvicorn main:app` 就启动完整后端服务

---

## 📊 优先级和依赖关系

```
Priority 0 (现在就做):
  Phase 1 ─ Prompt 架构升级            ← 基础工程，影响所有后续

Priority 1 (核心能力):
  Phase 2 ─ Decision Engine            ← 省 Token、降延迟
  Phase 3 ─ CITA 2.0                   ← 长对话刚需
  Phase 4 ─ Persona 角色系统升级        ← 项目核心卖点

Priority 2 (进阶能力):
  Phase 8 ─ 多层记忆升级               ← 用户粘性关键
  Phase 6 ─ 结构化输出                  ← 输出质量提升
  Phase 5 ─ 子 Agent 系统              ← 复杂任务支持

Priority 3 (锦上添花):
  Phase 7 ─ 调度系统                   ← 主动服务
  Phase 9 ─ 渠道抽象                   ← 未来扩展做准备
  Phase 10 ─ 测试体系                  ← 重构底气
  Phase 11 ─ 后端服务化                ← 部署上线前
```

---

## 📝 当前已完成（打底基础）

- ✅ ReAct Agent（LangGraph + 流式输出）
- ✅ UnifiedMiddleware 5 层（Gate → Policy → Timeout → CITA → Persona）
- ✅ Skill 系统（12 个 Skill，YAML frontmatter，关键词匹配）
- ✅ 基础记忆（SQLite 持久化 + ChromaDB RAG + 上下文裁剪）
- ✅ Persona 系统（4 角色 + 懒加载 + 切换）
- ✅ MCP 工具集成（高德天气/地图/搜索）
- ✅ 动漫数据爬虫（Bangumi + Yuc）
- ✅ Streamlit 前端（会话管理、知识库上传、Skill 列表）
- ✅ 虚拟环境 + requirements.txt + .gitignore
